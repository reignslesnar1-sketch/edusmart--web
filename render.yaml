services:
  - type: web
    name: edusmart-exam-system
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    port: 5000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
